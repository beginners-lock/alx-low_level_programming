#include "main.h"

/**
 * _abs - Entry
 * @i: as int
 * Description: prints absolute value of int
 * Return: absolute value
 */

int _abs(int i)
{

	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
